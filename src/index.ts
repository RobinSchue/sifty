#!/usr/bin/env node
// npm bin entry (see package.json "bin"). All behavior lives in cli.ts —
// this file exists only so the shebang and the published entry point are
// separate from the composition root itself.
import "./cli.js";
